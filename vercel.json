{
    "builds": [{
        "src": "AsnanDental/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "AsnanDental/wsgi.py"
        }
    ]
}
